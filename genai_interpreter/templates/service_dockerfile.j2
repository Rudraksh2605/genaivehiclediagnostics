FROM python:3.10-slim

WORKDIR /app

RUN pip install fastapi uvicorn pydantic scikit-learn numpy pandas

COPY vehicle_health_service.py .

EXPOSE 8001

CMD ["uvicorn", "vehicle_health_service:app", "--host", "0.0.0.0", "--port", "8001"]
